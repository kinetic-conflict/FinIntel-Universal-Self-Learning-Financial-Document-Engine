# FinIntel Validation Service

## Run
```bash
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 9002

Open:
http://127.0.0.1:9002/docs
http://127.0.0.1:9002/health

---

# Run it (commands)

Open terminal in `validation/` folder:

```bash
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --port 9002



